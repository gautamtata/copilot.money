{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "alembic upgrade head && hypercorn main:app --bind 0.0.0.0:$PORT --bind [::]:$PORT",
    "healthcheckPath": "/api/backend/health"
  }
}
