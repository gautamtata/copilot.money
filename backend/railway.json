{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "alembic upgrade head && uvicorn main:app --host :: --port $PORT",
    "healthcheckPath": "/api/backend/health"
  }
}
